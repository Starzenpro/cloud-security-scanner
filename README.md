# Multi-Cloud Security Scanner (AWS + Azure)

Automated security scanning for cloud infrastructure.

## Quick Start

1. Install dependencies:
   ```bash
   pip install -r requirements.txt# cloud-security-scanner
An automated cloud security scanner that detects misconfigurations in AWS infrastructure and Azure infrastructure, generates risk scores, and produces compliance-ready PDF reports.
